[package]
name = "harmony-filesystem"
version = "0.9.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["fs"] }
tempfile = "3.10"

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
tempfile = "3.10"
