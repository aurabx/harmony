apiVersion: v1
kind: Service
metadata:
  name: {{ include "harmony-proxy.fullname" . }}
  labels:
    {{- include "harmony-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "harmony-proxy.selectorLabels" . | nindent 4 }}
