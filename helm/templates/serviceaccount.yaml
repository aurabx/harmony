{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "harmony-proxy.serviceAccountName" . }}
  labels:
    {{- include "harmony-proxy.labels" . | nindent 4 }}
{{- end }}
